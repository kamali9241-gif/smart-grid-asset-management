# Copy to .env (or export directly) when running the backend outside docker-compose.
PORT=8080
DATABASE_URL=postgres://grid:grid@localhost:5432/grid_assets?sslmode=disable
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8081
LOG_LEVEL=info
MAX_UPLOAD_MB=25
